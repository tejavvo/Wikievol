# Copy this file to `.env` for local development.
#
# Base URL of the WikiEvolution Flask backend. Leave unset (or empty) in
# production, where the SPA and API are served from the same origin.
VITE_API_BASE_URL=http://localhost:5000
